#include <stdio.h>
int long long fact(int);
int main()
{
    int a;
    printf("Enter the number:");
    scanf("%d",&a);
    printf("The factorial of %d=%lld",a,fact(a));
    return 0;
}
int long long fact(int x)
{
    int f;
    if(x>=1)
    f=x*fact(x-1);
    else
    return 1;
return f;
}
